version: '3'
services:
  api:
    build: ./apps/api
    ports:
      - "3001:3001"
    env_file:
      - ./apps/api/.env
    volumes:
      - ./docs:/app/docs
      - ./docs_pending:/app/docs_pending

  web:
    build: ./apps/web
    ports:
      - "3000:3000"
    depends_on:
      - api
